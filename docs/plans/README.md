# Plans

- `otoml-migration/plan.md`: staged plan to migrate config handling from the
  current legacy `key=value` format to TOML using `otoml`.
- `otoml-migration/implementation-checklist.md`: file-by-file execution
  checklist for implementing the TOML migration.
